apiVersion: apps/v1
kind: Deployment
metadata:
  name: quote-app
spec:
  template:
    spec:
      containers:
      - name: app-container
        resources:
          limits:
            cpu: 500m
            memory: 512Mi